# Mission Control Instructions

- Treat `../full-version` as the canonical reference implementation for this project.
- For every thread in this workspace, use `../full-version` for component reuse, layout patterns, and design inspiration.
- Build and adapt features inside this `mission-control` app; do not replace the app wholesale with the reference project.
- Prefer lifting targeted components, sections, and styling ideas from `../full-version`, then tailoring them to the Mission Control product.
